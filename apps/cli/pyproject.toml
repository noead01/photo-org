[project]
name = "cli"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "api",
]

[tool.uv.sources]
api = { workspace = true }

[tool.setuptools.packages.find]
include = ["cli*"]

[project.scripts]
photo-org = "cli.__main__:main"
